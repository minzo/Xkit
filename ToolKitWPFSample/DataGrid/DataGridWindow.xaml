<Window x:Class="ToolKit.WPF.Sample.DataGridWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToolKit.WPF.Sample"
        mc:Ignorable="d"
        Title="DataGridWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:DataGridWindowViewModel />
    </Window.DataContext>

    <StackPanel>
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridHyperlinkColumn Header="HyperlinkColumn" Binding="{Binding Column0}" ContentBinding="{Binding Column1}" />
                <DataGridTemplateColumn Header="ExpanderColumn">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Expander Header="{Binding Column2[0]}" BorderThickness="1" BorderBrush="Black">
                                <ListBox ItemsSource="{Binding Column2}" Margin="16 0 0 0"/>
                            </Expander>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <Expander Header="{Binding Column2[0]}" BorderThickness="1" BorderBrush="Black">
                                <ListBox ItemsSource="{Binding Column2}" />
                            </Expander>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal">
            <Button Content="+" Width="20" Command="{Binding AddCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Items}" CanUserAddRows="False" />


    </StackPanel>
    

</Window>
